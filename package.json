{
  "name": "hf-dialog-selector",
  "version": "0.2.0",
  "private": true,
  "description": "Build tooling for the frontend CSS only — the app itself is Python.",
  "scripts": {
    "css": "tailwindcss -c tailwind.config.js -i app/static/css/tailwind.css -o app/static/css/app.css --minify",
    "css:watch": "tailwindcss -c tailwind.config.js -i app/static/css/tailwind.css -o app/static/css/app.css --watch"
  },
  "devDependencies": {
    "daisyui": "4.12.14",
    "tailwindcss": "3.4.17"
  }
}
